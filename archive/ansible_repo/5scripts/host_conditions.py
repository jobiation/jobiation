# Note that continue means skip.

        if str(arg1) != "1":
            continue;

        if str(active) != "1":
            continue;

        if str(devicename) == "devicename":
            continue;

        if str(devicename) == "skipline":
            continue;
